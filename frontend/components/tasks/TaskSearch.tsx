import { Search, X } from "lucide-react";
export default function TaskSearch({value,onChange}:{value:string;onChange:(v:string)=>void}){return <div className="relative flex-1"><Search size={17} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-[rgb(var(--muted))]"/><input value={value} onChange={e=>onChange(e.target.value)} placeholder="Search tasks by title..." className="block h-11 w-full rounded-xl border border-[rgb(var(--border))] bg-[rgb(var(--surface))] pl-10 pr-10 text-sm outline-none focus:border-[rgb(var(--primary)/.7)] focus:ring-4 focus:ring-[rgb(var(--primary)/.1)]" />{value&&<button onClick={()=>onChange("")} className="absolute right-2 top-1/2 grid h-8 w-8 -translate-y-1/2 place-items-center rounded-lg text-[rgb(var(--muted))] hover:bg-[rgb(var(--surface-muted))]" aria-label="Clear search"><X size={15}/></button>}</div>}
