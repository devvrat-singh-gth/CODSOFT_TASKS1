import { CalendarClock } from "lucide-react";
export default function DueDateBadge({date,status}:{date?:string|null;status?:string}){if(!date)return <span className="text-xs text-[rgb(var(--muted))]">No due date</span>;const due=new Date(date);const overdue=due.getTime()<Date.now()&&status!=="DONE";return <span className={overdue?"inline-flex items-center gap-1.5 text-xs font-medium text-[rgb(var(--danger))]":"inline-flex items-center gap-1.5 text-xs text-[rgb(var(--muted))]"}><CalendarClock size={14}/>{overdue?"Overdue":due.toLocaleDateString(undefined,{month:"short",day:"numeric"})}</span>}
