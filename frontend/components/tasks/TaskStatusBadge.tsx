import type { TaskStatus } from "@/types/task";
export default function TaskStatusBadge({status}:{status:TaskStatus}){const map={TODO:["To do","bg-[rgb(var(--surface-muted))] text-[rgb(var(--muted))]"],IN_PROGRESS:["In progress","bg-[rgb(var(--primary)/.1)] text-[rgb(var(--primary))]"],REVIEW:["Review","bg-violet-500/10 text-violet-600 dark:text-violet-400"],DONE:["Done","bg-emerald-500/10 text-emerald-600 dark:text-emerald-400"]} as const;const [label,cls]=map[status];return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${cls}`}>{label}</span>}
