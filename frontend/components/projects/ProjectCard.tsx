import Link from "next/link";
import { ArrowUpRight, CalendarDays, MoreHorizontal } from "lucide-react";
import type { Project } from "@/types/project";
import ProjectStatusBadge from "./ProjectStatusBadge";
import ProjectProgress from "./ProjectProgress";
export default function ProjectCard({project,onEdit,onDelete}:{project:Project;onEdit:()=>void;onDelete:()=>void}){const date=new Date(project.updatedAt).toLocaleDateString(undefined,{month:"short",day:"numeric",year:"numeric"});return <article className="group relative rounded-2xl border border-[rgb(var(--border))] bg-[rgb(var(--surface))] p-5 shadow-sm transition duration-200 hover:-translate-y-1 hover:shadow-lg"><div className="flex items-start justify-between gap-3"><div><ProjectStatusBadge status={project.status}/><Link href={`/projects/${project.id}`} className="mt-3 block text-lg font-semibold tracking-tight hover:text-[rgb(var(--primary))]">{project.title}</Link></div><div className="relative"><button type="button" aria-label="Project actions" className="grid h-9 w-9 place-items-center rounded-lg text-[rgb(var(--muted))] hover:bg-[rgb(var(--surface-muted))]" onClick={e=>{e.stopPropagation();const menu=e.currentTarget.nextElementSibling as HTMLElement|null;if(menu)menu.classList.toggle("hidden")}}><MoreHorizontal size={18}/></button><div className="hidden absolute right-0 top-10 z-10 w-32 rounded-xl border border-[rgb(var(--border))] bg-[rgb(var(--surface))] p-1 shadow-xl"><button onClick={onEdit} className="block w-full rounded-lg px-3 py-2 text-left text-sm hover:bg-[rgb(var(--surface-muted))]">Edit</button><button onClick={onDelete} className="block w-full rounded-lg px-3 py-2 text-left text-sm text-[rgb(var(--danger))] hover:bg-[rgb(var(--danger)/.08)]">Delete</button></div></div></div><p className="mt-2 min-h-10 line-clamp-2 text-sm leading-5 text-[rgb(var(--muted))]">{project.description || "No description added."}</p><div className="mt-5"><ProjectProgress project={project}/></div><div className="mt-5 flex items-center justify-between gap-3 text-xs text-[rgb(var(--muted))]"><span className="inline-flex items-center gap-1.5"><CalendarDays size={14}/>{date}</span><Link href={`/projects/${project.id}`} className="inline-flex items-center gap-1 font-semibold text-[rgb(var(--primary))]">Open<ArrowUpRight size={14}/></Link></div></article>}
