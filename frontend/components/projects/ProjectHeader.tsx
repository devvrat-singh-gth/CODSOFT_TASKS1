import { FolderKanban, Plus } from "lucide-react";
import Button from "@/components/ui/Button";
export default function ProjectHeader({count,onCreate}:{count:number;onCreate:()=>void}){return <div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><div className="mb-2 flex items-center gap-2 text-[rgb(var(--primary))]"><FolderKanban size={17}/><span className="text-xs font-semibold uppercase tracking-[.18em]">Workspace</span></div><h1 className="text-3xl font-semibold tracking-tight sm:text-4xl">Projects</h1><p className="mt-2 text-sm leading-6 text-[rgb(var(--muted))]">Organize the work behind each goal and keep progress visible.</p></div><Button size="md" onClick={onCreate}><Plus size={17}/>New project</Button><span className="sr-only">{count} projects</span></div>}
