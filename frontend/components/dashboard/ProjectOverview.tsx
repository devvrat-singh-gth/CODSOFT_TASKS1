import { CalendarDays, CheckCircle2 } from "lucide-react";
import type { Project } from "@/types/project";
export default function ProjectOverview({projects}:{projects:Project[]}){const active=projects.filter(p=>p.status!=="COMPLETED").length;const complete=projects.filter(p=>p.status==="COMPLETED").length;return <div className="grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-[rgb(var(--border))] bg-[rgb(var(--surface))] p-4"><p className="text-xs text-[rgb(var(--muted))]">Visible projects</p><p className="mt-2 text-2xl font-semibold">{projects.length}</p></div><div className="rounded-xl border border-[rgb(var(--border))] bg-[rgb(var(--surface))] p-4"><div className="flex items-center gap-2 text-xs text-[rgb(var(--muted))]"><CalendarDays size={14}/>Active</div><p className="mt-2 text-2xl font-semibold">{active}</p></div><div className="rounded-xl border border-[rgb(var(--border))] bg-[rgb(var(--surface))] p-4"><div className="flex items-center gap-2 text-xs text-[rgb(var(--muted))]"><CheckCircle2 size={14}/>Completed</div><p className="mt-2 text-2xl font-semibold">{complete}</p></div></div>}
